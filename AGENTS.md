# Avatar Studio

This is `dafepro/zmap-avatar-studio`, package `@zmap/avatar-studio`. It is independently owned and released; a parent zmap checkout may pin it as a Git submodule. Commit avatar changes here, then update the parent gitlink explicitly.

Read `README.md`, `docs/contracts.md`, `docs/accessory-fitting.md`, `docs/wielding.md` and `docs/motion.md` before changing their respective contracts. Keep identity, inventory, rewards, room transport and consuming-app UI out of the reusable runtime. Use approved concept references, interactive Blender and actual browser verification for asset changes. Preserve source provenance and fitting/animation invariants.

Commit and push only as `dafepro` (`313492551+dafepro@users.noreply.github.com`), using repository-local Git identity and credentials. Never change machine-wide accounts. Run relevant unit, type, build and browser/package checks. `public/` is shipped content; `assets/source/` is authoring material.
